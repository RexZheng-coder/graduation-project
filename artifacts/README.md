# Artifacts

This directory stores generated outputs from experiments and debugging sessions.

- `model-exports/` contains saved model files and hash metadata.
- `training-checkpoints/` contains checkpoint files produced during training.
- `tracking/` contains MLflow and Lightning tracking outputs.
