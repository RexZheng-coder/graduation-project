# Datasets

This directory stores local raw datasets used by the experiment folders.

- `fashion-mnist/` contains the downloaded FashionMNIST raw files.
- `mnist/` contains the downloaded MNIST raw files.
