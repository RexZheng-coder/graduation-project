# Experiments

This directory contains supporting experiments and prototype code that informed the final Re2po project.

- `mnist-mlflow/` is the MLflow and PyTorch Lightning training example area.
- `model-storage-prototypes/` contains early model decomposition and reconstruction scripts.
- `serverless-action-demos/` contains small action-style scripts used while exploring serverless execution.
