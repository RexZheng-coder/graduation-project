# Environments

This directory stores environment setup material used during development.

- `docker/` contains Dockerfiles and container build experiments.
- `snapshots/` contains Python environment snapshots captured during project work.
